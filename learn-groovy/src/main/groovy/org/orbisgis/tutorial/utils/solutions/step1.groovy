package org.orbisgis.tutorial.utils.solutions

import groovy.transform.BaseScript
import org.orbisgis.tutorial.utils.TutorialScript

@BaseScript TutorialScript tutorialScript

/**
 * Step 1 : Comments
 */

//Hello, I'm a comment. You are the only one which can read me, I'm ignored on execution.

/*
 * Hello again.
 * I'm also a comment.
 * But I can be written over more than one line
 */

/**
 * Still a comment, but with a prettier color.
 */

assert step == 1

next step